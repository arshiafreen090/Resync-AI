{
  "name": "resync-ai",
  "version": "1.0.0",
  "private": true,
  "description": "ReSync AI - Landing Page",
  "scripts": {
    "dev": "npm run dev --prefix apps/frontend",
    "start": "npm run start --prefix apps/frontend",
    "build": "npm run build --prefix apps/frontend",
    "lint": "npm run lint --prefix apps/frontend"
  },
  "workspaces": [
    "apps/frontend"
  ]
}