# ReSync AI - Libraries

Place third-party libraries (local scripts, icons, etc.) in this directory.

Currently, this project uses vanilla CSS and JavaScript with no external dependencies beyond Google Fonts.
